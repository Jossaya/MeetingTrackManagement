import { Talk } from '@/models/talk';
import { Meeting } from '@/models/meeting';

export class MeetingViewModel {
    public testInput: string[] = [];
    public testOutput: Meeting = new Meeting();
}
